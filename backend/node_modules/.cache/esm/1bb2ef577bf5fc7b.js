let JWT_SECRET,jwt;_7bf‍.x([["default",()=>_7bf‍.o]]);_7bf‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_7bf‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
    static sign(payload, expiry = '1y', secret = JWT_SECRET) {
        return jwt.sign(payload, secret, { expiresIn: expiry });
    }

    static verify(token, secret = JWT_SECRET) {
        return jwt.verify(token, secret);
    }
}

_7bf‍.d(JwtService);
